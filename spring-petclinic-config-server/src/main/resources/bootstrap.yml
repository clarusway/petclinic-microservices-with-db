server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/clarusway/petclinic-microservices-configs-with-db.git
---
spring:
  profiles: local
  cloud:
    config:
      server:
        git:
          uri: file:///${GIT_REPO}
